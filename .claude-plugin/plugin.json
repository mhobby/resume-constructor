{
  "name": "resume-constructor",
  "description": "Generate tailored CVs and cover letters from your professional profile. Built on the WAT framework — AI handles reasoning, deterministic scripts handle PDF rendering.",
  "version": "1.0.0",
  "author": {
    "name": "Matt Hobby"
  },
  "hooks": {
    "PostInstall": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(uv run:*)",
      "Bash(DYLD_LIBRARY_PATH=/opt/homebrew/lib uv run:*)"
    ]
  }
}
